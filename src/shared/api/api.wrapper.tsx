import { PropsWithChildren, useEffect } from "react"

export const APIWrapper = ({children}: PropsWithChildren) => {
    useEffect(() =>{

    },[])
    return (
        <>
            {children}
        </>
    )
}